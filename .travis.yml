# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
cache: packages
apt_packages:
 - python3
brew_packages:
 - python3
script:
 - travis_wait R -e 'devtools::test()'
r:
 - release
os:
 - linux
 - osx
