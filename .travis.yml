# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
cache: packages
r:
 - release
os:
 - linux
 - osx
after_success:
 - Rscript -e 'covr::codecov()'

script: - |
  R CMD build .
  travis_wait 20 R CMD check tsForecastR*tar.gz
